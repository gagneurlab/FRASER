context("Annotations methods")

test_that("annotateRanges", {
    expect_equal("ok", "ok")
    message("needs to be implemented ... :(")
    #annotation_dataset <- annotateRanges(fds)
    #expect_that(mcols(fds, type="psiSite")$hgnc_symbol,
    #    equals(rep(c("TMEM39A", "TIMMDC1", "ARHGEF18", "PCP2", "MCOLN1", "PNPLA6"), c(1,16,2,2,15,2)))
    #)
})
